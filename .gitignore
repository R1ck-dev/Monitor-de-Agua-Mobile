# --- Configuração local do Claude Code (skills, hooks, settings) e contexto interno ---
.claude/
CLAUDE.md

# --- Segredos e variáveis de ambiente ---
.env
.env.*
*.local
# ...mas o template de variáveis (sem segredos) deve ser versionado
!.env.example

# --- Arquivos de sistema operacional ---
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# --- IDEs ---
.idea/
*.iml
*.ipr
*.iws
.vscode/*
!.vscode/extensions.json
.settings/
.project
.classpath

# --- Flutter / Dart ---
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
build/
coverage/

# Symbolication / ofuscação de release
app.*.symbols
app.*.map.json

# Android/iOS: propriedades locais de máquina (o resto fica em android/.gitignore e ios/.gitignore)
android/local.properties
android/key.properties
**/*.keystore
**/*.jks
